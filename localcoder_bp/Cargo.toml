[package]
name = "localcoder_bp"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
trueos = { path = "../trueos" }

[features]
default = []
host-std = ["trueos/host-std"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = false
strip = false
