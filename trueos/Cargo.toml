[package]
name = "trueos"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[lib]
name = "trueos"
path = "src/lib.rs"

[dependencies]
trueos-sys = { path = "../trueos-sys" }

[features]
default = []
linked-portal = []
