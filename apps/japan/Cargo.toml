[package]
name = "japan"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "=1.52.3", default-features = false, features = ["macros", "rt-multi-thread", "sync", "time"] }
trueos = { workspace = true, features = ["tokio-runtime"] }
