[package]
name = "flags"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "flags"
path = "main.rs"

[dependencies]
reqwest = { version = "0.13.3", default-features = false, features = ["json", "rustls"] }
trueos.workspace = true
