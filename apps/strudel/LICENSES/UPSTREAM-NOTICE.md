# Upstream license notice

This archive does not initially contain the upstream Strudel bundle; the file
under `apps/strudel_core/js/vendor/` is a placeholder.

When `tools/vendor-strudel-core.mjs` is run:

- generated Strudel code is AGPL-3.0-or-later;
- Fraction.js is MIT;
- the script copies package license files into the vendor license directory
  when those files are present.

Keep those files and provide corresponding source when distributing a build
that embeds the generated bundle.
