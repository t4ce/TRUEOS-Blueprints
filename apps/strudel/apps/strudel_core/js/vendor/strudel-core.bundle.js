/*
 * PLACEHOLDER ONLY.
 *
 * `tools/vendor-strudel-core.mjs` overwrites this file with a self-contained,
 * pinned @strudel/core bundle. Until then, 00_fallback_core.js supplies a small
 * independently-written temporal compatibility kernel so the QJS/audio path is
 * testable without network access.
 */
globalThis.__TRUEOS_UPSTREAM_STRUDEL_PRESENT = false;
