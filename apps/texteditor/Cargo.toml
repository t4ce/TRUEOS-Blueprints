[package]
name = "texteditor"
version = "0.1.0"
edition = "2024"

[package.metadata.trueos-blueprint]
replicatable = true

[[bin]]
name = "texteditor"
path = "server.rs"

[dependencies]
axum.workspace = true
serde.workspace = true
serde_json.workspace = true
trueos = { workspace = true, features = ["lifecycle-net"] }
