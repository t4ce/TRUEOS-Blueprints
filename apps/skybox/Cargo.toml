[package]
name = "skybox"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "skybox"
path = "src/main.rs"

[dependencies]
libm.workspace = true
trueos = { workspace = true, features = ["ui4-scene"] }

[build-dependencies]
png = { version = "0.18.1", default-features = false }
