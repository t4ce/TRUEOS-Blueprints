[package]
name = "hello_world_replicatable"
version = "0.1.0"
edition = "2024"

[package.metadata.trueos-blueprint]
replicatable = true

[dependencies]
trueos = { workspace = true, features = ["tokio-net-probe"] }
