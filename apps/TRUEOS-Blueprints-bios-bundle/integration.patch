diff --git a/Cargo.toml b/Cargo.toml
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -7,6 +7,7 @@ edition = "2024"
 members = [
     ".",
     "api",
+    "apps/bios",
     "apps/cut_tetris",
     "apps/dobby",
     "probes/condvar",
diff --git a/crates/trueos-v/src/lib.rs b/crates/trueos-v/src/lib.rs
--- a/crates/trueos-v/src/lib.rs
+++ b/crates/trueos-v/src/lib.rs
@@ -38,6 +38,7 @@ pub mod qjs_abi;
 pub mod sync;
 pub mod varchive;
 pub mod vaudio;
+pub mod vbios;
 pub mod vcabi;
 pub mod vclock;
 pub mod vfetch;
diff --git a/crates/trueos-v/src/vbios.rs b/crates/trueos-v/src/vbios.rs
new file mode 100644
--- /dev/null
+++ b/crates/trueos-v/src/vbios.rs
@@
+extern crate alloc;
+
+use alloc::{string::String, vec, vec::Vec};
+
+type BiosReadFn = unsafe extern "C" fn(offset: usize, out_ptr: *mut u8, out_cap: usize) -> isize;
+
+unsafe extern "C" {
+    fn trueos_vlayer_bios_schema_snapshot_read(
+        offset: usize,
+        out_ptr: *mut u8,
+        out_cap: usize,
+    ) -> isize;
+}
+
+#[inline]
+pub fn snapshot_len() -> Result<usize, i32> {
+    read_len(trueos_vlayer_bios_schema_snapshot_read)
+}
+
+#[inline]
+pub fn snapshot_bytes() -> Result<Vec<u8>, i32> {
+    read_all(trueos_vlayer_bios_schema_snapshot_read)
+}
+
+#[inline]
+pub fn snapshot_text() -> Result<String, i32> {
+    String::from_utf8(snapshot_bytes()?).map_err(|_| -1)
+}
+
+fn read_all(read_fn: BiosReadFn) -> Result<Vec<u8>, i32> {
+    let len = read_len(read_fn)?;
+    let mut bytes = vec![0u8; len];
+    if len == 0 {
+        return Ok(bytes);
+    }
+
+    let got = unsafe { read_fn(0, bytes.as_mut_ptr(), bytes.len()) };
+    if got < 0 {
+        return Err(got as i32);
+    }
+
+    bytes.truncate((got as usize).min(len));
+    Ok(bytes)
+}
+
+fn read_len(read_fn: BiosReadFn) -> Result<usize, i32> {
+    let len = unsafe { read_fn(0, core::ptr::null_mut(), 0) };
+    if len < 0 {
+        return Err(len as i32);
+    }
+    Ok(len as usize)
+}
diff --git a/apps.json b/apps.json
--- a/apps.json
+++ b/apps.json
@@
    "badc",
    "calculator",
+    "bios",
    "cut_tetris",
diff --git a/apps/bios/server.rs b/apps/bios/server.rs
--- a/apps/bios/server.rs
+++ b/apps/bios/server.rs
@@
-const PORT: u16 = 1012;
+const PORT: u16 = 8338;
@@
-    let addr = SocketAddr::from(([127, 0, 0, 1], PORT));
+    let addr = SocketAddr::from(([0, 0, 0, 0], PORT));
     let listener = trueos::lifecycle_axum_listener!("bios", addr, &PUBLISHED_PORT).await;
     logl::log(
         level::INFO,
-        alloc::format!(
-            "bios: http://127.0.0.1:{}/ (read-only)",
+        format_args!(
+            "bios: http://0.0.0.0:{}/ (read-only)",
             PUBLISHED_PORT.load(Ordering::Acquire)
         ),
     );
     axum::serve(listener, router()).await
@@
-            "{\"ok\":true,\"service\":\"bios\",\"listen\":\"loopback\",\"port\":{port},\"readOnly\":true,\"activeWritePath\":\"none\"}}"
+            "{\"ok\":true,\"service\":\"bios\",\"listen\":\"0.0.0.0\",\"port\":{port},\"readOnly\":true,\"activeWritePath\":\"none\"}}"
         )
         .into_bytes(),
         true,
     )
@@
-            logl::log(level::ERROR, alloc::format!("bios: runtime build failed {err}"));
+            logl::log(level::ERROR, format_args!("bios: runtime build failed {err}"));
             return;
         }
     };
@@
         if let Err(err) = serve().await {
-            logl::log(level::ERROR, alloc::format!("bios: server failed {err:?}"));
+            logl::log(level::ERROR, format_args!("bios: server failed {err:?}"));
         }
     });
