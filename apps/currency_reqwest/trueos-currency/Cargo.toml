[package]
name = "trueos-currency"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
serde_json.workspace = true
trueos = { workspace = true, features = ["tokio-net-probe"] }