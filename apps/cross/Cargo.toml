[package]
name = "cross"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "cross"
path = "src/main.rs"

[dependencies]
crossbeam = { version = "0.8.4", default-features = false, features = ["alloc"] }
trueos.workspace = true
