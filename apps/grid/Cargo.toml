[package]
name = "grid"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[package.metadata.trueos-blueprint]
replicatable = false

[[bin]]
name = "grid"
path = "src/main.rs"

[dependencies]
trueos = { workspace = true, features = ["ui4-scene"] }
