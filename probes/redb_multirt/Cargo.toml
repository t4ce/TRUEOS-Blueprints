[package]
name = "redb_multirt"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "redb_multirt"
path = "main.rs"

[dependencies]
redb_probe = { path = "../redb_probe" }
trueos-redb.workspace = true
trueos.workspace = true
