[package]
name = "trueos"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[features]
default = []
default-global-allocator = []
default-panic-handler = []

[lib]
name = "trueos"
path = "src/lib.rs"

[dependencies]
v = { path = "../../../crates/trueos-v" }
