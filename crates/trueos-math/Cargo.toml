[package]
name = "trueos-math"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[lib]
path = "src/lib.rs"

[features]
default = []
alloc = []

[dependencies]
libm = { version = "0.2", default-features = false }
