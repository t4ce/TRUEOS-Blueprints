[package]
name = "rayon"
version = "1.12.0"
description = "Simple work-stealing parallelism for Rust"
documentation = "https://docs.rs/rayon/"
exclude = ["/ci/*", "/.github/*"]

categories.workspace = true
edition.workspace = true
keywords.workspace = true
license.workspace = true
readme.workspace = true
repository.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[features]
# This feature switches to a spin-lock implementation on the browser's
# main thread to avoid the forbidden `atomics.wait`.
#
# Only useful on the `wasm32-unknown-unknown` target.
web_spin_lock = ["dep:wasm_sync", "rayon-core/web_spin_lock"]

[dependencies]
# These are both public dependencies!
rayon-core = { version = "1.13.0", path = "rayon-core" }
either.workspace = true

wasm_sync = { workspace = true, optional = true }

[target.'cfg(target_os = "trueos")'.dependencies]
hashbrown.workspace = true

[dev-dependencies]
rand.workspace = true


[workspace]
members = ["rayon-demo", "rayon-core"]
exclude = ["ci"]

[workspace.package]
readme = "README.md"
rust-version = "1.85"
edition = "2024"
license = "MIT OR Apache-2.0"
repository = "https://github.com/rayon-rs/rayon"
keywords = ["parallel", "thread", "concurrency", "join", "performance"]
categories = ["concurrency"]

[workspace.lints.rust]
private-bounds = "deny"
private-interfaces = "deny"
unnameable-types = "deny"
unreachable-pub = "deny"
unsafe-op-in-unsafe-fn = "deny"

rust_2018_idioms = { level = "warn", priority = -1 }
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_os, values("trueos"))'] }

# Some dependencies may not be their latest version, in order to support older rustc.
[workspace.dependencies]
core3 = { version = "=0.1.2", default-features = false, features = ["alloc"] }
crossbeam-deque = { version = "=0.8.6", path = "../crossbeam-deque-0.8.6", default-features = false, features = ["alloc"] }
crossbeam-utils = { version = "=0.8.21", path = "../crossbeam-utils-0.8.21", default-features = false }
either = { version = "=1.16.0", default-features = false }
hashbrown = { version = "0.17.0", default-features = false, features = ["default-hasher"] }
libc = "=0.2.186"
rand = "=0.10.1"
scoped-tls = "=1.0.1"
spin = "=0.10.0"
v = { path = "../../crates/trueos-v", default-features = false }
wasm_sync = "=0.1.2"
