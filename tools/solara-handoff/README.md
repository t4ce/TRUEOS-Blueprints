# Solara Frame Protocol Lab

A dependency-free interactive architecture page for the retained CPU/GPU state
handoff proposed for Solara and TRUEOS graphics.

Open `index.html` directly, or serve this directory with any static HTTP server.
The four scenarios demonstrate an accepted frame, a rejected stale epoch, deferred
resource retirement, and safe promotion of an incorrect damage region.
